# ===========================================
# Condominios Manager - Production Requirements
# ===========================================
# Compatible with Python 3.12, 3.13, and 3.14
# Install: py -m pip install -r requirements.txt (Windows)
# Install: pip install -r requirements.txt (Linux/Docker)

# Core Django Dependencies
# Django 5.1+ required for Python 3.14 compatibility
Django>=5.1.0,<6.0
djangorestframework>=3.14.0,<3.16
django-cors-headers>=4.3.1,<5.0

# API Documentation
drf-spectacular>=0.28.0,<0.29

# Authentication
djangorestframework-simplejwt>=5.3.1,<5.4
# django-allauth with socialaccount extra (required for Google OAuth JWT handling)
django-allauth[socialaccount]>=0.61.1,<0.70
cryptography>=42.0.0,<47.0

# Configuration Management
python-decouple>=3.8,<4.0
python-dotenv>=1.0.1,<2.0

# Database
# Use psycopg (v3) which has better Python 3.14 support
# Falls back to psycopg2-binary on older systems
psycopg[binary]>=3.1.0,<4.0

# Caching
django-redis>=5.4.0,<6.0
redis>=5.0.0,<6.0

# PDF Generation
pyppeteer>=2.0.0,<3.0
reportlab>=4.1.0,<5.0
# Pillow 11.x required for Python 3.14 compatibility
Pillow>=11.0.0,<12.0

# Utilities
num2words>=0.5.14,<0.6
python-dateutil>=2.8.0,<3.0
requests>=2.31.0,<3.0
Jinja2>=3.1.0,<4.0

# Logging
python-json-logger>=3.0.0,<4.0

# Production Server (Linux only - ignored on Windows)
gunicorn>=23.0.0,<24.0; sys_platform != "win32"

# Async support for pyppeteer
websockets>=10.4,<14.0
