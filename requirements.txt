# ===========================================
# Condominios Manager - Production Requirements
# ===========================================
# Compatible with Python 3.12, 3.13, and 3.14
# Install: uv pip install -r requirements.txt
# Install: pip install -r requirements.txt

# Core Django Dependencies
# Django 5.2 LTS supports Python 3.14 (added in 5.2.8)
Django>=5.2.0,<6.0
djangorestframework>=3.15.0,<3.16
django-cors-headers>=4.6.0,<5.0

# API Documentation
drf-spectacular>=0.28.0,<0.30

# Authentication
djangorestframework-simplejwt>=5.3.1,<5.6
# django-allauth with socialaccount extra (required for Google OAuth JWT handling)
django-allauth[socialaccount]>=0.63.0,<0.70
cryptography>=44.0.0,<47.0

# Configuration Management
python-decouple>=3.8,<4.0
python-dotenv>=1.0.1,<2.0

# Database
# psycopg (v3) — native Python 3.14 support
psycopg[binary]>=3.2.0,<4.0

# Caching
django-redis>=5.4.0,<7.0
redis>=5.0.0,<6.0

# PDF Generation
# Playwright replaces pyppeteer (unmaintained since 2021)
playwright>=1.50.0,<2.0
reportlab>=4.1.0,<5.0
Pillow>=11.0.0,<13.0

# Utilities
num2words>=0.5.14,<0.6
python-dateutil>=2.8.0,<3.0
requests>=2.31.0,<3.0
Jinja2>=3.1.0,<4.0

# Logging
python-json-logger>=3.0.0,<4.0

# WhatsApp / SMS
twilio>=9.0.0,<10.0

# Production Server (Linux only - ignored on Windows)
gunicorn>=23.0.0,<26.0; sys_platform != "win32"

# Production Infrastructure
sentry-sdk>=2.0.0,<3.0
whitenoise>=6.8.0,<7.0
django-storages[s3]>=1.14.0,<2.0

# Websockets (used by Playwright internally)
websockets>=14.0,<17.0
